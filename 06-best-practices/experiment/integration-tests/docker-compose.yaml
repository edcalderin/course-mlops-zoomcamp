services:
  backend:
    image: ${LOCAL_IMAGE_NAME}
    ports:
      - 8080:8080
    environment:
      - PREDICTIONS_STREAM_NAME=ride_predictions
      - RUN_ID=Test123
      - TEST_RUN=True
      - MODEL_LOCATION=/app/model
    volumes:
      - "./model:/app/model"